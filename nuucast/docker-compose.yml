services:
  media-server:
    build: .
    container_name: nuucast
    ports:
      - "80:3000"
    volumes:
      - ./media:/media:ro
    environment:
      - RUST_LOG=info
      - SERVER_HOST=0.0.0.0
      - SERVER_PORT=3000
    restart: unless-stopped
